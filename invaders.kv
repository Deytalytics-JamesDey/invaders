<InvadersGame>:
    player_ship: player_ship

    Ship:
        id: player_ship
        center_x: root.center_x
        center_y: 0 + self.height

    Label:
        id: scorelabel
        pos: 20, root.height-70
        font_size: 25
        text: "SCORE: "+ str(root.score)

    Label:
        id: wavelabel
        pos: root.width-100, root.height-70
        font_size: 25
        text: "WAVE: "+ str(root.wave)

    Button:
        id: btnLeft
        pos: (0, 1)
        on_press: root.leftButton()
        Image:
            source: 'images/arrow_left.png'
            allow_stretch: True
            keep_ratio: False


    Button:
        id: btnRight
        pos: (root.width-100, 1)
        on_press: root.rightButton()
        Image:
            source: 'images/arrow_right.png'
            allow_stretch: True
            keep_ratio: False

<Fleet>:

    Label:
        id: gameover
        pos: (root.width/2), (root.height)
        font_size: 25
        text: " "


<Invader>:
    size: 50, 50

    canvas:
        Ellipse:
            pos: self.pos
            size: self.size
            source: self.image

<Ship>:
    size: 50, 50

    canvas:
        Ellipse:
            pos: self.pos
            size: self.size
            source: self.image

<Bullet>:
    size: 5, 5

    canvas:
        Ellipse:
            pos: self.pos
            size: self.size



